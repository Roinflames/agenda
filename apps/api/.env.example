# Prisma reads .env from the working directory.
# For local dev, you can copy the repo root .env into apps/api/.env:
#
#   cp ../../.env .env
#
DATABASE_URL="postgresql://USER:PASSWORD@HOST/DB?sslmode=require"

