{
  "name": "boxmagic-saas",
  "version": "0.5.1",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "npm run dev -w apps/api",
    "dev:all": "npm run dev -w apps/api & npm run dev -w apps/web",
    "dev:api": "npm run dev -w apps/api",
    "dev:web": "npm run dev -w apps/web",
    "dev:mobile": "npm run dev -w apps/mobile",
    "start": "node apps/api/dist/src/main.js",
    "start:api": "node apps/api/dist/src/main.js",
    "build": "npm run prisma:generate -w apps/api && npm run build -w apps/api && npm run build -w apps/web",
    "test": "npm run test -w apps/api && npm run test -w apps/web",
    "lint": "npm run lint -w apps/api && npm run lint -w apps/web",
    "docker:up": "docker compose up -d --build",
    "docker:down": "docker compose down -v",
    "db:migrate": "npm run prisma:migrate -w apps/api",
    "db:seed": "npm run prisma:seed -w apps/api",
    "demo:check-reports": "node scripts/check-reports-demo.mjs"
  }
}
